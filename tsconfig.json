{
  "compilerOptions": {
    // 编译后的 JS 标准
    "target": "ES5",
    // 生成的模块系统
    "module": "commonjs",
    // 是否生成声明文件以便在 JS 和 TS 项目中同时使用
    "declaration": true,
    // 编译目录 tsconfig.json 同级下的 lib/
    "outDir": "./lib",
    // 是否启用所有严格类型检查
    "strict": true,
    // 编译所需库
    "lib": [
      "es6"
    ],
    "esModuleInterop": true
  },
  // 指定编译目录
  "include": [
    "src"
  ],
  // 指定排除不编译的
  "exclude": [
    "node_modules/",
    "**/__test__/**"
  ],
}
